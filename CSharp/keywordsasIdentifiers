 public static void usekeywordsAsIdentifiers()
        {
            // key words are resorved words in c# they can not be 
            // used as identifiers, it is not recommended. 
            // but c# has a provision to use them by prefixing with @
            int @public = 11;
            Console.WriteLine(@public);
        }
